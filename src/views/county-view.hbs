{{#if superAdmin}}
  {{> admin-menu active="dashboard"}}
{{else}}
  {{>  menu active="dashboard"}}
{{/if}}

<section class="section page-margin">
  <div class="title">
    {{county.county}}
  </div>
  {{> list-clubs}}
  {{> add-club}}

</section>