{{#if UserLoggedIn}}
  {{#if superAdmin}}
    {{> admin-menu active="clubs"}}
  {{else}}
    {{>  menu active="clubs"}}
  {{/if}}
{{else}}
  {{>  welcome-menu active="clubs"}}
{{/if}}

{{#if clubDetails}}
  {{> club-details }} 
{{else}}
  {{> club }}
{{/if}}
