{{#if UserLoggedIn}}
  {{#if superAdmin}}
    {{> admin-menu active="about"}}
  {{else}}
    {{>  menu active="about"}}
  {{/if}}
{{else}}
  {{>  welcome-menu active="about"}}
{{/if}}


<section class="section">
  <h1> Hello from RugbyGamePOI </h1>
</section>
