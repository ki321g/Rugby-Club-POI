{{#if superAdmin}}
  {{> admin-menu active="dashboard"}}
{{else}}
  {{>  menu active="dashboard"}}
{{/if}}

<section class="section">
  {{#if clubAdded}}
    {{> club-image }}  
  {{else}}
    {{#if hideAddClub}}
        {{> list-clubs }}
      {{else}}
        {{> add-club-dashboard }}
      {{/if}}  
  {{/if}}  
</section>
