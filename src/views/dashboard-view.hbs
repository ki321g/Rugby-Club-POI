{{#if superAdmin}}
  {{> admin-menu active="dashboard"}}
{{else}}
  {{>  menu active="dashboard"}}
{{/if}}

<section class="section pt-4">   
  {{#if clubAdded}}
    {{> club-image }}  
  {{else}}
    {{#if hideAddClub}}
        {{> list-clubs }}
      {{else}}
        {{> add-club-dashboard }}
      {{/if}}  
  {{/if}}  
</section>
